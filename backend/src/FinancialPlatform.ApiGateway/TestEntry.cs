namespace FinancialPlatform.ApiGateway;

// Marker class used by integration tests with WebApplicationFactory<TestEntry>
// so the test project doesn't need extern alias to reference Program
public class TestEntry { }
